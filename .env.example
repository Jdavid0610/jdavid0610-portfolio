# --- Public ---------------------------------------------------------------
# The canonical origin, with no trailing slash. Everything derives from it:
# metadataBase, canonical URLs, hreflang, OG images, sitemap, robots, llms.txt
# and every `@id` in the JSON-LD graph. Nothing in the source hardcodes an origin.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
